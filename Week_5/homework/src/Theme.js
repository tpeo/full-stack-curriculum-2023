// Theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  // You can update this part to customize your theme
  palette: {
    primary: {
      main: "#BF5700", // This is an example color, set to UT's Burnt Orange. Change it and see what happens!
    },
    secondary: {
      main: "#FF8787", // This is another example color.
    },
  },
});

export default theme;
